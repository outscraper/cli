# Outscraper Businesses CSV Starter

This template pulls business records and writes a CSV file.

## Setup

```bash
npm install
cp .env.example .env
node export-businesses.js
```

The script writes `businesses.csv` in the current folder.
